% Hand-maintained wrapper: caption + table environment live here.
% Numbers are generated into table_cknna_matrix_data.tex by scripts/make_cknna_matrix.py.
\begin{table}[tbp]
\centering
\small
\setlength{\tabcolsep}{8pt}
\input{tables/table_cknna_matrix_data.tex}
\caption{Every REPA variant raises per-residue alignment to all three encoders, including the two it was never aligned to. Across-layer peak per-residue CKNNA (bootstrap median, $\times 10^{3}$), with the peak trunk layer as a superscript; $k{=}10$, step 1.0M, $n{\le}256$ PDB-trained. The $\dagger$ marks each model's own alignment target. Absolute values are small (an order of magnitude below image-domain REPA), so we read the pattern, not the magnitude. ($n{=}1$ seed; bootstrap medians.)}
\label{tab:cknna-matrix}
\end{table}
