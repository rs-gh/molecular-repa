% Hand-maintained wrapper: caption + table environment live here.
% Numbers are generated into table_rep_quality_full_data.tex by scripts/make_rep_quality_full.py.
\begin{table}[tbp]
\centering
\small
\setlength{\tabcolsep}{5pt}
\input{tables/table_rep_quality_full_data.tex}
\caption{Full PDB representation-quality ablation across all six variants. The remaining encoder$\times$depth combinations behind Table~\ref{tab:proteina-rep}, at the same 1.0M checkpoint. The encoder-routed pattern holds: GearNet wins the fold probes (CATH C/A/T), ProteinMPNN edges the inverse-folding probe (IF). At matched layer~4, the trained GearNet (L4-GearNet) beats the random control (L4-random) on every probe, so the learned-versus-random gap is not an artefact of injection depth. ($n{=}3$ seeds, $\pm$ half the range; ($^{\dagger}$) L4-GearNet and L4-MPNN single-seed. Best-layer linear probe.)}
\label{tab:proteina-rep-full}
\end{table}
