% Hand-maintained wrapper: caption + table environment live here.
% Numbers are generated into table_rep_quality_data.tex by scripts/make_rep_quality.py.
\begin{table}[tbp]
\centering
\small
\setlength{\tabcolsep}{5pt}
\input{tables/table_rep_quality_data.tex}
\caption{\textbf{REPA's representation advantage is encoder-routed.} REPA-GearNet wins the fold probes (CATH C/A/T) decisively, while REPA-MPNN edges the per-residue probe (IF). The random control gains least and does not help IF, evidence of genuine structural transfer rather than regularisation. (PDB-trained; means over $n{=}3$ seeds at 1.0M; best-layer linear probe (protocol in Appendix~\ref{app:leakage}). Per-seed ranges and remaining variants in Appendix Table~\ref{tab:proteina-rep-full}.)}
\label{tab:proteina-rep}
\end{table}
