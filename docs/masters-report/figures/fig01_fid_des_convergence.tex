% Figure -- headline FID + designability convergence. \input{} from Ch6.
\begin{figure}[tbp]
\centering
\includegraphics[width=\textwidth]{figures/fig01_fid_des_convergence.png}
\caption{\textbf{REPA accelerates FPSD in both data regimes, and designability on experimental (PDB) data.} The strongest variant in each regime is shown, with the random-encoder control in grey. PDB retains a persistent designability gap, but its baseline appears to catch up on FPSD after $\approx$1M steps; we examine this in \S\ref{sec:proteina-convergence}. (Each point is a mean over $3$ seeds, except a few single-seed checkpoints at the trajectory tails; $1{,}125$ backbones/seed, $250$ for designability.)}
\label{fig:proteina-fid}
\end{figure}
