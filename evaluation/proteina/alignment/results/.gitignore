# Cached intermediate features and frozen batches are large (~1 GB total)
# and trivially regenerated from the pipeline scripts. Don't track in git.
*.pt
encoder_features/
model_features/

# Duplicate of frozen_batch_n10k.json written by build_or_load_manifest; the
# friendly-named copy is the one we track.
batch_manifest_cknna_n10k_v1.json

# Tracked headline artifacts:
#   cknna_matrix_{per_residue,per_protein}.jsonl  — the two matrices
#   frozen_batch_n10k.json                        — exact protein sample (repro)
#   figures/*.png
!cknna_matrix_per_residue.jsonl
!cknna_matrix_per_protein.jsonl
!frozen_batch_n10k.json
!figures/
